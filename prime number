#include<stdio.h>
main()
{
int i,n,c;
c=0;
printf("\n enter the number:");
scanf("%d",&n);
for(i=1;i<=n;i++)
{if(n%i==0)
{c++;
}
}
if(c==2)
{printf("\n %d is an prime number",n);
}
else
{
printf("\n %d is not an prime number",n);}
}
