#include<stdio.h>
int fib(int n)
{
int fib[n+2];
int i,f[n];
f[0]=0;
f[1]=1;
for(i=2;i<=n;i++)
{
f[i]=f[i-1]+f[i-2];
}
return f[n];
}
int main()
{
int n=8;
printf("%d",fib(n));
getchar();
return 0;
}
